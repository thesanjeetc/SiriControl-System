moduleName = "templateModule"
commandWords = ["xxx"]

def execute(command):
    #Write anything you want to be execute when the commandWords are heard
    #The 'command' parameter is the command you speak
    return
